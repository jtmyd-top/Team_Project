# 🧪 群组加入申请弹窗功能 - 快速测试指南

## 前置准备

### 1. 重启服务
```bash
# 重启 Django 开发服务器
python manage.py runserver

# 如果使用 Daphne（WebSocket）
daphne -b 0.0.0.0 -p 8000 knowledge_project.asgi:application
```

### 2. 准备测试账号
- **账号A**: 群主或管理员
- **账号B**: 普通用户（用于提交申请）

---

## 测试场景

### ✅ 场景 1: 进入群聊时自动弹窗

**步骤：**
1. 使用账号B，访问需要审批的群组邀请链接
2. 提交加入申请（填写留言）
3. 使用账号A（群主/管理员）登录
4. 打开私信页面
5. **点击该群对话**

**预期结果：**
- ✅ 立即弹出"待审核申请"对话框
- ✅ 显示账号B的申请信息（头像、用户名、留言、时间）
- ✅ 有"通过"和"拒绝"按钮
- ✅ 有"24小时内不再提醒"和"7天内不再提醒"选项

**如果失败：**
- 检查控制台是否有错误日志
- 确认账号A确实是群主或管理员
- 检查申请状态是否为 `pending`

---

### ✅ 场景 2: 实时推送弹窗（重点！）

**步骤：**
1. 使用账号A（群主/管理员）登录
2. 打开私信页面
3. **进入该群聊页面**（重要：必须在群聊页面）
4. 打开另一个浏览器窗口或隐身模式
5. 使用账号B提交加入申请

**预期结果：**
- ✅ 账号A的页面**立即弹出**待审核提醒（无需刷新）
- ✅ 如果弹窗已经打开，新申请会追加到列表顶部
- ✅ "待审核"标签的红色徽章数字自动增加

**如果失败：**
- 打开浏览器控制台，查看 Network 标签
- 检查 WebSocket 连接状态（应该显示 `ws://localhost:8000/ws/messages/`）
- 查看 Console 是否有 WebSocket 相关错误
- 确认 Django Channels 配置正确

---

### ✅ 场景 3: 审批操作

**在弹窗中操作：**

**测试 3.1: 通过申请**
1. 点击"通过"按钮
2. **预期结果：**
   - ✅ 该申请从弹窗列表中移除
   - ✅ 账号B收到"入群申请已通过"的通知
   - ✅ 账号B成为群成员
   - ✅ "待审核"标签数字减1
   - ✅ 如果没有其他申请，弹窗自动关闭

**测试 3.2: 拒绝申请**
1. 点击"拒绝"按钮
2. 系统提示输入拒绝原因
3. 填写原因（例如："暂不接受新成员"）
4. **预期结果：**
   - ✅ 该申请从弹窗列表中移除
   - ✅ 账号B收到"入群申请被拒绝"的通知及原因
   - ✅ "待审核"标签数字减1

---

### ✅ 场景 4: 暂停提醒功能

**步骤：**
1. 进入群聊触发弹窗
2. 点击"24小时内不再提醒"
3. 关闭弹窗
4. 退出群聊再次进入

**预期结果：**
- ✅ 不再弹出提醒
- ✅ 控制台显示：`群组 {groupId} 的提醒已暂停`
- ✅ 待审核申请仍然存在（只是不弹窗）
- ✅ 24小时后会再次提醒

**清除暂停状态：**
```javascript
// 在浏览器控制台执行
localStorage.clear()
// 或者清除特定群组
localStorage.removeItem('group_123_reminder_snooze')
```

---

### ✅ 场景 5: 权限控制

**步骤：**
1. 使用账号C（普通成员，非管理员）登录
2. 进入群聊

**预期结果：**
- ✅ 不显示待审核提醒弹窗
- ✅ "待审核"标签不显示

---

### ✅ 场景 6: 实时推送（在其他页面）

**步骤：**
1. 使用账号A（群主/管理员）登录
2. 打开私信页面，**不要**进入群聊
3. 使用账号B提交加入申请

**预期结果：**
- ✅ 页面右上角显示消息通知："用户XXX 申请加入群组 XXX"
- ✅ "待审核"标签的红色徽章数字自动增加
- ✅ 不会弹出对话框（因为不在该群聊页面）

---

## 🐛 故障排查

### 问题 1: 弹窗不显示

**检查清单：**
```
□ 账号是否为群主或管理员
□ 是否有待审核申请（status = 'pending'）
□ 是否被暂停提醒（检查 localStorage）
□ 控制台是否有 JavaScript 错误
□ API 请求是否返回 200 状态码
```

**调试命令：**
```javascript
// 在浏览器控制台
// 1. 检查是否有暂停提醒
localStorage.getItem('group_123_reminder_snooze')

// 2. 手动清除暂停状态
localStorage.clear()

// 3. 检查当前对话
window.$vm?.selectedConversationKey

// 4. 检查权限
window.$vm?.canManageCurrentGroup
```

---

### 问题 2: WebSocket 不推送

**检查清单：**
```
□ WebSocket 连接是否成功（查看 Network 标签）
□ Django Channels 是否正确配置
□ Redis/内存 Channel Layer 是否运行
□ 后端是否有异常日志
```

**调试步骤：**
1. 打开浏览器控制台
2. 切换到 Network 标签
3. 筛选 WS (WebSocket)
4. 查看 `/ws/messages/` 连接状态
5. 点击连接查看收发的消息

**预期看到的消息：**
```json
// 发送 ping
{"type": "ping"}

// 收到 pong
{"type": "pong"}

// 收到申请通知
{
  "type": "group_join_request",
  "group_id": 123,
  "group_name": "群名称",
  "request_id": 456,
  "user": {
    "id": 789,
    "username": "申请人",
    "avatar": "/path/to/avatar.jpg"
  },
  "request_message": "我想加入"
}
```

---

### 问题 3: 后端错误

**查看 Django 日志：**
```bash
# 开发服务器会直接显示错误
python manage.py runserver

# 检查是否有导入错误
python manage.py check

# 测试 WebSocket 消费者
python manage.py shell
>>> from messaging.consumers import ChatConsumer
>>> # 应该不报错
```

---

## 📊 测试检查表

| 测试项 | 预期结果 | 实际结果 | 备注 |
|--------|----------|----------|------|
| 进入群聊自动弹窗 | ✅ | ⬜ | |
| 实时推送立即弹窗 | ✅ | ⬜ | |
| 通过申请 | ✅ | ⬜ | |
| 拒绝申请 | ✅ | ⬜ | |
| 暂停提醒 24h | ✅ | ⬜ | |
| 暂停提醒 7天 | ✅ | ⬜ | |
| 权限控制（普通成员） | ✅ | ⬜ | |
| 其他页面消息通知 | ✅ | ⬜ | |
| 待审核标签计数同步 | ✅ | ⬜ | |
| WebSocket 连接正常 | ✅ | ⬜ | |

---

## 🎬 演示视频建议

录制以下场景展示功能：

1. **场景 1** (10秒): 进入群聊自动弹窗
2. **场景 2** (15秒): 实时推送弹窗（分屏显示两个账号）
3. **场景 3** (10秒): 快速审批（通过/拒绝）
4. **场景 4** (5秒): 暂停提醒功能

---

## 📞 需要帮助？

如果测试遇到问题，请提供：
1. 浏览器控制台的完整错误日志（Console 标签）
2. WebSocket 连接状态（Network > WS 标签）
3. Django 后端日志
4. 测试的具体步骤和预期结果

---

**测试完成后，请删除测试数据，避免影响生产环境！**
